server:
  port: 8084

spring:
  application:
    name: inventory-service
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - configserver:${CONFIG_SERVER_URL}
  data:
    mongodb:
      uri: ${MONGO_URI}
