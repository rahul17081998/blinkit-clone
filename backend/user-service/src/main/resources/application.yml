server:
  port: 8082

spring:
  application:
    name: user-service
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - configserver:${CONFIG_SERVER_URL}
  data:
    mongodb:
      uri: ${MONGO_URI}
