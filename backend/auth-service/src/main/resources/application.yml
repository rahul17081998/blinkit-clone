server:
  port: 8081

spring:
  application:
    name: auth-service
  config:
    import:
      - optional:file:.env[.properties]       # when CWD = backend/ (start-backend.sh)
      - optional:file:../.env[.properties]    # when CWD = service dir (IntelliJ)
      - configserver:${CONFIG_SERVER_URL}
  data:
    mongodb:
      uri: ${MONGO_URI}                        # resolved from .env or OS env
