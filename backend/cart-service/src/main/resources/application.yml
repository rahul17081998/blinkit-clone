server:
  port: 8087

spring:
  application:
    name: cart-service
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - configserver:${CONFIG_SERVER_URL}

feign:
  circuitbreaker:
    enabled: true
